import { PlatziClass } from "./main.js";

const clase67 = new PlatziClass({
  name: "JavaScript: orientado a objetos, basado en prototipos",
  videoID: "a-valid-video-id", // replace with a valid video ID
});

clase67.reproducir();
clase67.pausar();
clase67.reproducir();
clase67.pausar();
clase67.reproducir();
clase67.pausar();